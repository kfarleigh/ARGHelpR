% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identify_argcandidates_divergence.R
\name{identify_argcandidates_divergence}
\alias{identify_argcandidates_divergence}
\title{Identify ancestral recombination graphs under selection that correspond to models of divergence.}
\usage{
identify_argcandidates_divergence(
  dat,
  analysis = "all",
  background = NULL,
  tmrca.threshold = NULL,
  pop1.threshold = NULL,
  pop2.threshold = NULL,
  tmrca.iqr.low.threshold = NULL,
  tmrca.iqr.high.threshold = NULL
)
}
\arguments{
\item{dat}{a dataframe of ancestral recombination graph statistics calculated using the argstats function.}

\item{analysis}{a character vector indicating the types of args you want to identify. Options are "all" for all types, "RI", "WP", "RS", "RIWP".}

\item{background}{a dataframe of ancestral recombination graph statistics calculated using the argstats function. If supplied this will be used to define thresholds and the dat argument will be treated as the test set.}

\item{tmrca.threshold}{a numeric value to customize the tmrca threshold used to identify ARGs associated with reproductive isolation.}

\item{pop1.threshold}{a numeric value to customize the pop1 tmrcaw threshold used to identify ARGs associated with within-population selection and recurrent selection.}

\item{pop2.threshold}{a numeric value to customize the pop2 tmrcaw threshold used to identify ARGs associated with within-population selection and recurrent selection.}

\item{tmrca.iqr.low.threshold}{a numeric value to customize the lower tmrca threshold used to identify ARGs associated with within-population selection and recurrent selection.}

\item{tmrca.iqr.high.threshold}{a numeric value to customize the upper tmrca threshold used to identify ARGs associated with within-population selection and recurrent selection.}
}
\value{
a list containing ARGs that were identified as candidates
}
\description{
Identify ancestral recombination graphs under selection that correspond to models of divergence.
}
\examples{
\donttest{
Test <- identify_argcandidates(dat = rattlesnake_argstats, analysis = "all")}
}
